/**
 * Created by liyan on 16/5/4.
 */
import PixelRatio from 'PixelRatio';

const Scale = 1;// / PixelRatio.get();
const BigSize = 72 * Scale;
const MiddleSize = 54 * Scale;
const SmallSize = 36 * Scale;
const DotSize = 60 * Scale;

export {
    Scale,
    BigSize,
    MiddleSize,
    SmallSize,
    DotSize,
}
